---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: joplin-database
  namespace: joplin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: flux-managed-storage-class
  resources:
    requests:
      storage: 1Gi
