---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: joplin-database
  labels:
    type: nfs
spec:
  storageClassName: flux-managed-storage-class
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    server: ourbackup.home
    path: /volume1/k8s-storage/active/joplin-database
