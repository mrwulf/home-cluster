---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: box-db-init
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-login
  target:
    name: box-db-init-secret
    template:
      data:
        INIT_POSTGRES_DBNAME: "box"
        INIT_POSTGRES_HOST: "postgres16-rw.databases.svc.cluster.local"
        INIT_POSTGRES_USER: "{{ .POSTGRES_USER }}"
        INIT_POSTGRES_PASS: "{{ .POSTGRES_PASS }}"
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
  data:
    - secretKey: POSTGRES_SUPER_PASS
      remoteRef:
        key: "${BW_POSTGRES_SUPERUSER}"
        property: "password"
    - secretKey: POSTGRES_USER
      remoteRef:
        key: "${BW_BOX_DB_CREDENTIALS}"
        property: "username"
    - secretKey: POSTGRES_PASS
      remoteRef:
        key: "${BW_BOX_DB_CREDENTIALS}"
        property: "password"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: box
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-login
  target:
    name: box-secret
    template:
      data:
        # Homebox environment variables
        HBOX_DATABASE_DRIVER: "postgres"
        HBOX_DATABASE_DATABASE: "box"
        HBOX_DATABASE_HOST: "postgres16-rw.databases.svc.cluster.local"
        HBOX_DATABASE_PORT: "5432"
        HBOX_DATABASE_USERNAME: "{{ .POSTGRES_USER }}"
        HBOX_DATABASE_PASSWORD: "{{ .POSTGRES_PASS }}"
        HBOX_DATABASE_SSL_MODE: "disable"

        # OIDC/Authentik variables
        HBOX_OIDC_ENABLED: "true"
        HBOX_OIDC_ISSUER_URL: "https://auth.${SECRET_DOMAIN}/application/o/box/"
        HBOX_OIDC_CLIENT_ID: "{{ .OIDC_CLIENT_ID }}"
        HBOX_OIDC_CLIENT_SECRET: "{{ .OIDC_CLIENT_SECRET }}"
        HBOX_OIDC_SCOPE: "openid profile email"
        HBOX_OPTIONS_TRUST_PROXY: "true"
  data:
    - secretKey: POSTGRES_USER
      remoteRef:
        key: "${BW_BOX_DB_CREDENTIALS}"
        property: "username"
    - secretKey: POSTGRES_PASS
      remoteRef:
        key: "${BW_BOX_DB_CREDENTIALS}"
        property: "password"
    - secretKey: OIDC_CLIENT_ID
      remoteRef:
        key: "${BW_BOX_OIDC_CREDENTIALS}"
        property: "username"
    - secretKey: OIDC_CLIENT_SECRET
      remoteRef:
        key: "${BW_BOX_OIDC_CREDENTIALS}"
        property: "password"
