---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tandoor-db
  namespace: household
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: ceph-block
