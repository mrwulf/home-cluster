---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMStaticScrape
metadata:
  name: ingress-vps-primary
  namespace: networking
spec:
  jobName: ingress-vps-primary
  targetEndpoints:
    - targets:
        - "${VPS_PRIMARY_PUBLIC_IP}:8082"
      scheme: "http"
      path: "/metrics"
      interval: "30s"
      relabelConfigs:
        - targetLabel: cluster
          replacement: ingress-vps
        - targetLabel: service
          replacement: traefik
    - targets:
        - "${VPS_PRIMARY_PUBLIC_IP}:9091"
      scheme: "http"
      path: "/metrics"
      interval: "30s"
      relabelConfigs:
        - targetLabel: cluster
          replacement: ingress-vps
        - targetLabel: service
          replacement: netbird-relay
    - targets:
        - "${VPS_PRIMARY_PUBLIC_IP}:9100"
      scheme: "http"
      path: "/metrics"
      interval: "30s"
      relabelConfigs:
        - targetLabel: cluster
          replacement: ingress-vps
        - targetLabel: service
          replacement: node-exporter
---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMStaticScrape
metadata:
  name: ingress-vps-backup
  namespace: networking
spec:
  jobName: ingress-vps-backup
  targetEndpoints:
    - targets:
        - "${VPS_BACKUP_PUBLIC_IP}:8082"
      scheme: "http"
      path: "/metrics"
      interval: "30s"
      relabelConfigs:
        - targetLabel: cluster
          replacement: ingress-vps
        - targetLabel: service
          replacement: traefik
    - targets:
        - "${VPS_BACKUP_PUBLIC_IP}:9091"
      scheme: "http"
      path: "/metrics"
      interval: "30s"
      relabelConfigs:
        - targetLabel: cluster
          replacement: ingress-vps
        - targetLabel: service
          replacement: netbird-relay
    - targets:
        - "${VPS_BACKUP_PUBLIC_IP}:9100"
      scheme: "http"
      path: "/metrics"
      interval: "30s"
      relabelConfigs:
        - targetLabel: cluster
          replacement: ingress-vps
        - targetLabel: service
          replacement: node-exporter
