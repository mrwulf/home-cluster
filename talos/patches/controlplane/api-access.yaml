machine:
  features:
    kubernetesTalosAPIAccess:
      enabled: true
      allowedRoles:
        - os:etcd:backup
        - os:admin
      allowedKubernetesNamespaces:
        - backups
        - system-upgrade
